const assert = require('chai').assert;

describe ('testing', () => {

  it ('matching number of outputs and number of files', () => {

  });

  // it ('', () => {
  //
  // });
  //
  // it ('', () => {
  //
  // });

});
